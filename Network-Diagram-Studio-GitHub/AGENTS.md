# Generated production package

This is generated release output, not the editable application source.
The maintained source is in the sibling `..\network-diagram-studio` directory.
Read its `AGENTS.md` and release instructions before making application changes.
If that source is unavailable, obtain it rather than patching the bundled HTML.

Every application change needs a new version and a localized What's new entry
with reviewed before/after images. Keep the shared centered 960 x 760 maximum
history layout consistent; do not add per-release styles.

Use the source release checks, seal the reviewed candidate, stage and test both
editions, then update this complete package and the source non-production entry.
Update `Network Diagram Studio.bat` and both readmes to the same version,
preserving BAT CRLF. Finalize the source publication record only after verifying
both installed editions.

Do not modify unrelated runtime assets, remove older history, bypass the release
checks or publish remotely without explicit permission. Release images must
contain only synthetic examples, never customer data.

Current package: v0.9.117.
